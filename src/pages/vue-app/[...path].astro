---
import App from '../../vue-app/App.vue';

export function getStaticPaths() {
	return [
		{ params: { path: "about"}},
		{ params: { path: "blog"}},
		{ params: { path: 'test' }},
		{ params: { id: undefined}},
	];
}

---

<App client:only="vue"></App>